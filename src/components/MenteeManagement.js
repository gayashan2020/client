import React, { useEffect, useState, useContext } from "react";
import { Container } from "@mui/material";

const MenteeManagement = ({ userId }) => {
  return <Container maxWidth="lg"></Container>;
};

export default MenteeManagement;
